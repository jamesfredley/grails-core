description: Adds support for JSON Views to the project
build:
    plugins:
        - org.grails.plugins.views-json
dependencies:
    - scope: build
      coords: "org.grails.plugins:views-gradle:2.3.2"
    - scope: implementation
      coords: "org.grails.plugins:views-json"
    - scope: implementation
      coords: "org.grails.plugins:views-json-templates"
