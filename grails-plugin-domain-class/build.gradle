dependencies {

    implementation platform(project(':grails-bom'))

    api project(":grails-core"), project(":grails-spring")
    api project(":grails-plugin-validation")
    api "org.apache.groovy:groovy:$groovyVersion"

    // Datastore APIs
    api "org.grails:grails-datastore-gorm-validation"
    api("org.grails:grails-datastore-gorm") {
        transitive = false
    }
    api("org.grails:grails-datastore-gorm-support") {
        transitive = false
    }
    api "org.grails:grails-datastore-core"

    compileOnly 'jakarta.servlet:jakarta.servlet-api'
    compileOnly 'org.springframework:spring-test', {
        // MockHttpServletRequest/Response/Context used in many classes
    }

    testImplementation 'jakarta.servlet:jakarta.servlet-api'
    testImplementation 'org.springframework:spring-test'
}
