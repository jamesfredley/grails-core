description: Adds Asset Pipeline to a Grails Plugin for packaging
build:
    plugins:
        - asset-pipeline
dependencies:
    - scope: build
      coords: 'cloud.wondrify:asset-pipeline-gradle'
    - scope: runtimeOnly
      coords: "cloud.wondrify:asset-pipeline-grails"
