plugins {
    id 'groovy'
    id 'java-library'
}

version = projectVersion
group = 'org.apache.grails'

ext {
    pomArtifactId = 'grails-testing-support-views-gson'
    pomDescription = 'JSON Views Testing Support'
}

dependencies {
    implementation platform("org.apache.grails:grails-bom:$grailsVersion"), {
        // as a safeguard, exclude projects in this repo that included transitively to ensure we pick the right version
        exclude group: 'org.apache.grails.views', module: 'grails-views-core'
        exclude group: 'org.apache.grails', module: 'grails-testing-support-views-gson'
        exclude group: 'org.apache.grails', module: 'grails-scaffolding'
    }

    api project(':grails-gson') // Used in public API
    api 'org.springframework:spring-web' // Used in public API
    api 'org.apache.grails.data:grails-datastore-core' // Used in public API
    api 'org.spockframework:spock-core' // Used in public API

    implementation 'org.apache.grails:grails-core'
    implementation 'org.apache.grails.testing:grails-testing-support-core'
    implementation 'org.apache.grails.web:grails-web-url-mappings'
    implementation 'org.apache.groovy:groovy'
    implementation 'org.apache.groovy:groovy-json'
    implementation 'org.apache.groovy:groovy-templates'
    implementation 'org.springframework:spring-webmvc'
}

apply from: rootProject.layout.projectDirectory.file('gradle/java-config.gradle')
apply from: rootProject.layout.projectDirectory.file('gradle/test-config.gradle')
apply from: rootProject.layout.projectDirectory.file('gradle/publish-config.gradle')