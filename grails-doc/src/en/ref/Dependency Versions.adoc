== Dependency Versions for grails-bom

Grails applications rely on a set of dependencies managed through a Bill of Materials (BOM). The application bom is `grails-bom`. This BOM serves as a centralized location to declare and manage versions of dependencies used within a Grails applications. Understanding these dependencies and their versions is crucial for effective project management and dependency resolution.
